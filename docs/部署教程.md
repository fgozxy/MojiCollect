# 🚀 日语抽卡学习游戏 - 部署教程

本教程将指导你如何将日语抽卡学习游戏部署到各种平台上。

## 📋 目录
- [准备工作](#准备工作)
- [Cloudflare Pages 部署](#cloudflare-pages-部署)
- [其他平台部署](#其他平台部署)
- [常见问题](#常见问题)

## 🔧 准备工作

### 1. 获取项目代码
```bash
# 克隆仓库
git clone https://github.com/fgozxy/MojiCollect.git
cd MojiCollect

# 或者下载 ZIP 文件并解压
```

### 2. 本地测试（推荐）
```bash
# 安装依赖
npm install

# 启动本地服务器
npm run dev

# 在浏览器中打开 http://localhost:3000
```

### 3. 检查项目文件
确保你的项目包含以下核心文件：
```
MojiCollect/
├── index.html          # 主页面 ✅
├── css/style.css       # 样式文件 ✅
├── js/app.js          # 主应用逻辑 ✅
├── js/dataManager.js  # 数据管理 ✅
├── js/gameLogic.js    # 游戏逻辑 ✅
├── js/audioPlayer.js  # 音频播放 ✅
├── package.json       # 项目配置 ✅
└── README.md         # 说明文档 ✅
```

## ☁️ Cloudflare Pages 部署（最推荐）

### 🎯 方法一：GitHub 自动部署（零配置）

#### 步骤 1：Fork 仓库
1. 访问：https://github.com/fgozxy/MojiCollect
2. 点击右上角 **Fork** 按钮
3. 等待 Fork 完成

#### 步骤 2：连接 Cloudflare Pages
1. 访问：https://dash.cloudflare.com/pages
2. 登录你的 Cloudflare 账号
3. 点击 **Create a project**
4. 选择 **Connect to Git**
5. 选择你刚 Fork 的仓库
6. 点击 **Begin setup**

#### 步骤 3：配置部署
```
Project name: MojiCollect (或自定义)
Production branch: main
Root directory: / (保持默认)
Build command: npm run build (可以留空，因为是静态网站)
Build output directory: / (保持默认)
Environment variables: (无需设置)
```

#### 步骤 4：部署
1. 点击 **Save and Deploy**
2. 等待部署完成（通常 1-2 分钟）
3. 🎉 你的网站已上线！

访问：`https://你的仓库名.pages.dev`

---

### 🎯 方法二：手动上传文件

#### 步骤 1：准备文件
```bash
# 克隆或下载项目
git clone https://github.com/fgozxy/MojiCollect.git
cd MojiCollect

# 可选：删除不需要的文件以减小大小
rm -rf node_modules .git
```

#### 步骤 2：上传
1. 访问：https://dash.cloudflare.com/pages
2. 点击 **Create a project**
3. 选择 **Upload assets**
4. 拖拽整个项目文件夹
5. 点击 **Deploy site**

---

### 🎯 方法三：Wrangler CLI

#### 步骤 1：安装工具
```bash
# 安装 Wrangler
npm install -g wrangler

# 验证安装
wrangler --version
```

#### 步骤 2：登录 Cloudflare
```bash
wrangler login
# 浏览器会打开，点击授权
```

#### 步骤 3：部署
```bash
# 方法 1：直接部署
wrangler pages deploy --project-name=mojicollect

# 方法 2：使用项目脚本
npm run deploy
```

## 🌐 其他平台部署

### GitHub Pages
1. 进入你的 GitHub 仓库
2. 点击 **Settings**
3. 找到 **Pages** 选项
4. 设置：
   - Source: Deploy from a branch
   - Branch: main
   - Folder: /root
5. 点击 **Save**
6. 等待部署完成

### Netlify
1. 访问：https://app.netlify.com/drop
2. 拖拽整个项目文件夹
3. 自动部署完成
4. 获得随机域名，可自定义

### Vercel
1. 访问：https://vercel.com
2. 点击 **New Project**
3. 导入 GitHub 仓库
4. 自动检测为静态网站
5. 点击 **Deploy**

### Surge.sh
```bash
# 安装 Surge
npm install -g surge

# 部署
surge --domain mojicollect.surge.sh
```

### Firebase Hosting
```bash
# 安装 Firebase CLI
npm install -g firebase-tools

# 初始化
firebase init hosting

# 部署
firebase deploy
```

## 🛠️ 高级配置

### 自定义域名（Cloudflare Pages）

1. 在 Cloudflare Pages 项目设置中
2. 点击 **Custom domains**
3. 添加你的域名
4. 配置 DNS 记录

### 环境变量
```bash
# 在部署时设置环境变量
wrangler pages deploy --env=production
```

### 构建优化
如果需要构建优化，可以修改 `package.json`：
```json
{
  "scripts": {
    "build": "echo 'Optimizing...' && npm run optimize",
    "optimize": "echo 'Static site, no build needed'"
  }
}
```

## 🔍 常见问题

### Q: 部署后样式丢失？
**A**: 检查 CSS 文件路径是否正确，确保 `css/style.css` 文件存在。

### Q: 音频无法播放？
**A**: 确保音频文件格式正确，某些平台可能需要 HTTPS。

### Q: 本地运行正常，部署后有问题？
**A**: 检查浏览器控制台错误信息，通常是路径问题。

### Q: 如何更新已部署的网站？
**A**:
- GitHub 自动部署：推送到 main 分支即可
- 手动上传：重新上传文件
- CLI：重新运行部署命令

### Q: Cloudflare Pages 部署失败？
**A**:
1. 检查仓库是否包含 `index.html`
2. 确保没有语法错误
3. 查看部署日志获取详细错误信息

### Q: 如何自定义域名？
**A**:
1. 在部署平台添加自定义域名
2. 配置 DNS 记录
3. 等待 SSL 证书生成

## 📊 部署平台对比

| 平台 | 免费额度 | 自定义域名 | HTTPS | CI/CD | 推荐度 |
|------|----------|------------|-------|-------|--------|
| Cloudflare Pages | 无限 | ✅ | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| GitHub Pages | 1GB | ✅ | ✅ | ✅ | ⭐⭐⭐⭐ |
| Netlify | 100GB | ✅ | ✅ | ✅ | ⭐⭐⭐⭐ |
| Vercel | 100GB | ✅ | ✅ | ✅ | ⭐⭐⭐⭐ |
| Surge | 无限 | ✅ | ✅ | ❌ | ⭐⭐⭐ |

## 🎉 部署成功后

恭喜！你的日语抽卡学习游戏已经上线了！现在你可以：

1. **分享链接**给朋友
2. **添加自定义域名**
3. **设置分析**监控访问情况
4. **定期更新**内容

## 🆘 需要帮助？

如果遇到部署问题，可以：
1. 查看各平台的官方文档
2. 检查项目的 Issues 页面
3. 创建新的 Issue 描述问题

---

**祝你的日语学习之旅愉快！** 🎴✨